# Study Notes Summarizer — 업데이트 내역

**업데이트 내역**들을 모아 한 파일로 정리한 문서입니다.

---

## 업데이트 내역

| 버전 | 날짜 | 항목 | 내용 |
|------|------|------|------|
| V5.5 | 2026-02-12 | 목록 불러오기 개선 | 대시보드 컴포넌트가 마운트될 때와 뒤로 가기 캐시(cache)로 페이지를 복원할 때 목록들을 다시 불러오는 사항 개선. |
| V5.4 | 2026-02-12 | LLM 프롬포트 개선 | LLM이 의도를 더욱 정확히 파악할 수 있도록 컨텍스트 주입 방식과 제약 조건을 고도화. |
| V5.0 | 2026-02-11 | 대시보드 노트 카드 UI 개선 | 즐겨찾기 버튼을 제목 우측으로 이동. 비교용 체크박스를 제목과 같은 줄에 배치(체크박스·제목·상태 칩·즐겨찾기 한 줄). 완료/상태 뱃지를 제목과 같은 줄·제목 우측에 배치하고, 모바일에서 칩 크기·가로 폭 축소(w-fit, 반응형 패딩/폰트). |
| V5.0 | 2026-02-11 | 모바일 비교하기 영역 정렬 | 노트 2개 선택 시 "2개의 노트가 선택되었습니다"와 비교하기/선택 취소 버튼 정렬 개선. 모바일에서 문장과 버튼을 두 줄로 배치(flex-col), 버튼 텍스트 줄바꿈 방지(whitespace-nowrap). |
| V5.0 | 2026-02-11 | 듀얼 뷰 카테고리 칩 분리 표시 | 비교 화면(듀얼 뷰)에서 노트별 카테고리를 한 줄 합침이 아닌 항목별 칩으로 분리 표시. 각 카테고리 ID마다 개별 span으로 렌더링, flex-wrap으로 정돈. |
| V4.0 | 2026-02-10 | 노트 다중 카테고리 지원 | 노트가 여러 카테고리에 속할 수 있도록 `note_categories` 조인 테이블 기반 `category_ids` 구조를 대시보드·수업 자료·프로젝트·그래프·비교 뷰 전역에 반영. 서버에서 notes와 note_categories를 머지해 `category_ids` 배열을 만들고, 기존 `category_id` 값이 있을 때는 함께 포함되도록 호환 처리. |
| V4.0 | 2026-02-10 | 카테고리 사이드바 실시간 연동 | `categories` 테이블에 대한 Supabase Realtime 구독을 대시보드·수업 자료·프로젝트(목록/상세) 페이지에 추가. 한 화면에서 카테고리 이름·순서·삭제를 변경하면 다른 화면의 사이드바 카테고리도 자동으로 동기화되도록 개선. |
| V4.0 | 2026-02-10 | 수업 자료 링크 추가/수정 UX 개선 | 수업 자료 페이지에서 "링크 추가" 버튼이 링크 수정 중에도 항상 보이도록 변경. 수정 중에 "링크 추가"를 누르면 현재 수정 내용은 저장하지 않고 닫은 뒤, 바로 새 링크 추가 폼을 열도록 동작 통일. 사이드바에서 "수업 자료" 메뉴를 다시 눌렀을 때 링크 추가/수정 폼과 그룹·소그룹·링크 펼침 상태를 초기화해 첫 화면으로 돌아가도록 처리. |
| V4.0 | 2026-02-10 | 대시보드 노트 카드 액션 정렬 및 카테고리 이름 툴팁 | 노트 카드 우측의 즐겨찾기·카테고리·삭제 버튼을 한 줄로 정렬하고 아이콘 크기와 간격을 통일해 정돈된 UI로 개선. 노트에 적용된 카테고리 뱃지가 길어져 잘리는 경우, 마우스 오버 시 전체 카테고리 이름이 툴팁으로 보이도록 `title` 속성 추가. |
| V3.0 | 2026-02-09 | 즐겨찾기 기능 | 노트별 즐겨찾기 토글 추가. `notes.is_favorite` 컬럼 및 마이그레이션 `013_notes_favorite.sql`. PATCH `/api/notes/[id]`에 `is_favorite` 지원. 대시보드·노트 상세에서 별 버튼으로 토글, 낙관적 업데이트로 즉시 반응. 상단 툴바 "즐겨찾기만" 필터, 정렬 시 즐겨찾기 노트 우선. |
| V3.0 | 2026-02-09 | 사이드바 카테고리 즐겨찾기 묶음 | 왼쪽 카테고리란에 미분류와 같이 '즐겨찾기' 항목 추가. 선택 시 즐겨찾기한 노트만 표시, 개수 배지 표시. 카테고리 목록 내 즐겨찾기 항목은 텍스트만 표시(별 아이콘 없음). `?category=_favorites` 로 진입 가능. |
| V2.5 | 2026-02-09 | 수업 자료 링크 그룹/소그룹 구조 개선 | 그룹과 소그룹을 별도 SQL 테이블(`link_groups`, `link_subgroups`)로 전환하여 정규화. 그룹/소그룹 이름 수정 및 삭제 기능 추가. 소그룹 제목 글씨 크기 조정. 링크 수정 시 자동 스크롤. 링크 정렬 순서 변경. |
| V2.0 | 2026-02-09 | 코드 정리 및 개선 | `categoryErrorMessage` 함수를 `lib/utils/errors.ts`로 이동하여 재사용 가능하게 개선. `any` 타입을 `unknown`으로 변경하여 타입 안정성 향상. 에러 처리 일관성 개선. `as any` 타입 단언 제거. |
| V2.0 | 2026-02-09 | 카테고리 UI 개선 | 카테고리 순서 변경 기능 추가: 드래그 앤 드롭 지원, 위/아래 버튼으로 한 칸씩 이동. 통합 메뉴 버튼 추가(위/아래/수정/삭제). 사이드바 너비 조정. |
| V1.0 | 2026-02-04 | 노트 목록 페이지네이션 | 보기 개수(1/3/5/7/10개·전체) 선택, 이전/다음·페이지 번호로 이동. 9페이지 초과 시 첫·마지막·현재±2만 표시(예: 1 … 4 5 6 7 … 20). |
| V1.0 | 2026-02-04 | 대시보드 학습 통계 | 상단 4개 카드: 총 노트, 수업 자료 링크, 프로젝트, 공부 시간. 수업 자료·프로젝트 카드 클릭 시 해당 페이지 이동. 서버·포커스 시 개수 갱신. |
| V1.0 | 2026-02-04 | 공부 시간 | sessionStorage로 세션 시작 시각 저장. 다른 페이지 갔다 와도 같은 탭에서 누적. |
| V1.0 | 2026-02-04 | 그래프 뷰 터치 | 모바일에서 한 손가락 드래그로 그래프 패닝. touchstart/touchmove(passive:false)/touchend. 노드/링크 영역은 패닝 제외. |
| V1.0 | 2026-02-04 | README 전체 업데이트 | 업데이트 목차·사이트 개요·제공 기능·디렉터리 구조·라우팅·API 경로 등 모든 섹션 최신화. |

---